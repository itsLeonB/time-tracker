{
    "configurations": [
        {
            "name": "Time Tracker",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "envFile": "${workspaceFolder}/.env",
            "program": "${workspaceFolder}/cmd/app/main.go",
            "cwd": "${workspaceFolder}"
        }
    ]
}
